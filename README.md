# labox

Linear Algebra sandbox.

## Usage

Open `index.html` in a browser to run the app.

The app starts with a 3×3 matrix. You can:

- edit any cell value
- add or remove rows and columns
- swap two rows
- scale a row by a number
- add one row to another with an optional multiplier
